{
  "name": "ad-player",
  "version": "1.1.0",
  "description": "ad vip player",
  "main": "index.js",
  "scripts": {
    "watch": "rimraf ./dist && webpack -w",
    "build": "rimraf ./dist && webpack",
    "server": "browser-sync start --server --files \"**/**\" --index \"./index.html\"",
    "start": "npm-run-all --parallel watch server"
  },
  "keywords": [
    "player"
  ],
  "author": "fujun-iri",
  "license": "MIT",
  "devDependencies": {
    "file-loader": "^0.9.0",
    "html-loader": "^0.4.4",
    "npm-run-all": "^3.1.1",
    "rimraf": "^2.5.4",
    "webpack": "^1.13.3"
  },
  "dependencies": {
    "browser-sync": "^2.18.1",
    "core-js": "^2.4.1",
    "raw-loader": "^0.5.1"
  }
}
